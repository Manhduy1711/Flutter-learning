import "package:flutter/material.dart";

class Setting extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Phần này vẫn chưa được phát triển",),
      ),
    );
  }
}